# Apify Actor API Input Schema Notes

Generated by `docs/apify/generate-api-input-schema.py`.

Source: Apify public API `GET /v2/acts/{actor}` plus latest build `GET /v2/actor-builds/{buildId}`. These are developer notes for implementing deterministic HuntBrief adapters.

Test HuntBrief settings used for schema review: roles `AI Engineer`, `AI Automation`; seniority `Entry level`, `Associate`; experience `0–1 year`, `1–3 years`; salary `Any`; include keywords `AI`, `Artificial Intelligence`, `Automation`, `AI Engineer`; avoid keywords `none`; posted within `1 week`; locations `United Kingdom`, `New Zealand`.

## 54 Career Sites

- Mode: Standard
- Actor: `fantastic-jobs/career-site-job-listing-api`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `timeRange` | string | `7d` | enum: 1h, 24h, 7d, 6m | Select the time range for job listings. You can choose between hourly (1h), daily (24h), weekly (7d), or backfill of all active jobs (great to get st… |
| `limit` | integer | `10` | min 10; max 5000 | Maximum number of jobs to return per run. The minimum is 10 and the maximum is 5,000. Please set the memory to 1GB for runs above 2,000 jobs. |
| `includeAi` | boolean | `True` |  | BETA Feature: Include AI enriched fields. We enrich jobs with AI to retrieve relevant data from the job description. Please note that this performed … |
| `includeLinkedIn` | boolean | `` |  | BETA Feature: Include LinkedIn company data. We use AI to map each job to a LinkedIn organization. This is a beta so there might be some errors. We m… |
| `titleSearch` | array | `` |  | Array of job titles to search for. Use :* for prefix matching (e.g., 'Soft:*' will match 'Software', 'Softball', etc.) |
| `titleExclusionSearch` | array | `` |  | Array of job titles to exclude. Use :* for prefix matching (e.g., 'Soft:*' will match 'Software', 'Softball', etc.) |
| `locationSearch` | array | `` |  | Array of locations to search for. Location search uses phrase matching, so you must use the exact 'City, State/Region, Country' format. All locations… |
| `locationExclusionSearch` | array | `` |  | Array of locations to exclude. Location search uses phrase matching, so you must use the exact 'City, State/Region, Country' format. All locations us… |
| `descriptionSearch` | array | `` |  | Warning, this search might be too intenstive for the 6m backfill API. Please be very specific, limit your searches to a handful of keywords, and comb… |
| `descriptionExclusionSearch` | array | `` |  | Warning, this search might be too intenstive for the 6m backfill API. Please be very specific, limit your searches to a handful of keywords, and comb… |
| `organizationSearch` | array | `` |  | Array of organization names to search for. Use :* for prefix matching (e.g., 'Google:*' will match 'Google', 'Google Cloud', etc.) Getting several co… |
| `organizationExclusionSearch` | array | `` |  | Array of organization names to exclude. Use :* for prefix matching (e.g., 'Google:*' will match 'Google', 'Google Cloud', etc.) Getting several compa… |
| `domainFilter` | array | `` |  | Array of company domains to search for. Requires an exact match. |
| `domainExclusionFilter` | array | `` |  | Array of company domains to exclude. Requires an exact match. |
| `descriptionType` | string | `text` | enum: text, html | Format of the job description |
| `ats` | array | `` |  | Filter jobs by one ore more ATS. |
| `atsExclusionFilter` | array | `` |  | Exclude jobs from one or more ATS. |
| `aiEmploymentTypeFilter` | array | `` |  | BETA Feature: Filter by employment type |
| `aiWorkArrangementFilter` | array | `` |  | BETA Feature: Filter by work arrangement. Remote OK = remote with an office available. Remote Solely = remote with no office available. Include both … |
| `aiHasSalary` | boolean | `` |  | BETA Feature: Filter for jobs with salary information only. Set to false to include all jobs. Results include jobs that have either an AI enriched sa… |
| `aiExperienceLevelFilter` | array | `` |  | BETA Feature: Filter by years of experience |
| `aiVisaSponsorshipFilter` | boolean | `` |  | BETA Feature: Filter for jobs offering visa sponsorship only. Set to false to include all jobs. |
| `aiTaxonomiesFilter` | array | `` |  | BETA Feature: Filter by AI taxonomies |
| `aiTaxonomiesPrimaryFilter` | array | `` |  | BETA Feature: Filter by primary AI taxonomy |
| `aiTaxonomiesExclusionFilter` | array | `` |  | BETA Feature: Exclude jobs by AI taxonomies. Warning: this filter is very broad and will exclude jobs that are not in any of the selected taxonomies.… |
| `populateAiRemoteLocation` | boolean | `False` |  | If enabled, populates ai_remote_location with locations_derived when ai_remote_location is empty. |
| `populateAiRemoteLocationDerived` | boolean | `False` |  | If enabled, populates ai_remote_location_derived with locations_derived when ai_remote_location_derived is empty. |
| `removeAgency` | boolean | `` |  | Filter out recruitment agencies |
| `liIndustryFilter` | array | `` |  | BETA Feature: Filter by LinkedIn industries |
| `liOrganizationEmployeesLte` | integer | `` | min 0 | BETA Feature: Maximum number of employees in the company |
| `liOrganizationEmployeesGte` | integer | `` | min 0 | BETA Feature: Minimum number of employees in the company |
| `remote only (legacy)` | boolean | `` |  | Filter for remote jobs only. Set to false to include all jobs. This filter is very sensitive and will include jobs that have 'remote' in the title, d… |
| `datePostedAfter` | string | `` |  | We don't recommend using this parameter if you retrieve jobs on a regular interval with the 1h/24h/7d time range. Filter jobs posted on or after a sp… |

### HuntBrief adapter hints

roles → `titleSearch`; locations → `locationSearch`; include/avoid → `descriptionSearch`/exclusions; posted within 1 week → `timeRange: 7d`; max uses `limit` but actor minimum is 10.

## Indeed

- Mode: Standard
- Actor: `misceres/indeed-scraper`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `position` | string | `` |  | Any combination of positions or keywords for search. Can be used together with Start URLs - both will be scraped independently, each limited by Max j… |
| `maxItemsPerSearch` | integer | `` | min 1 | Maximum number of job listings to scrape for each keyword search and each Start URL. When both are used together, each is limited independently by th… |
| `country` | string | `US` | enum: AQ, AR, AU, AT, BH, BE, BR, CA, CL, CN, CO, CR… | Country for job search. |
| `location` | string | `` |  | Any combination of city, zip code or locality for search. Used together with position/keywords. |
| `parseCompanyDetails` | boolean | `False` |  | If true, will also navigate to company page of each job posting, to scrape company info not available directly on job posting page. |
| `saveOnlyUniqueItems` | boolean | `True` |  | If true, only unique job listings will be scraped. |
| `followApplyRedirects` | boolean | `False` |  | If true, will follow redirects of Indeed's externalApplyLink and output the final one. |
| `startUrls` | array | `` |  | List of URLs that will be scraped. Can be main website URL, category/search URLs, company jobs URL (in the format of `https://www.indeed.com/cmp/Goog… |

### HuntBrief adapter hints

primary role query → `position`; country should be a supported country code such as `GB`/`NZ`; location string → `location`; max → `maxItemsPerSearch`.

## LinkedIn

- Mode: Standard
- Actor: `fantastic-jobs/advanced-linkedin-job-search-api`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `timeRange` | string | `7d` | enum: 1h, 24h, 7d, 6m | Select the time range for job listings. You can choose between hourly (1h), daily (24h), weekly (7d), or all active jobs (great to get started, last … |
| `limit` | integer | `10` | min 10; max 5000 | Maximum number of jobs to return per run. The minimum is 10 and the maximum is 5,000. Please set the memory to 1GB for runs above 2,000 jobs. |
| `includeAi` | boolean | `True` |  | BETA Feature: Include AI enriched fields. We enrich jobs with AI to retrieve relevant data from the job description. Please note that this performed … |
| `removeAgency` | boolean | `` |  | Filter out recruitment agencies, job boards and other low quality sources |
| `recruiterOnly` | boolean | `False` |  | Only return jobs that include a `recruiter_url` (a LinkedIn profile link to the recruiter who posted the job) ⚠️ Enabling this option triggers an add… |
| `titleSearch` | array | `` |  | Array of job titles to search for. Use :* for prefix matching (e.g., 'Soft:*' will match 'Software', 'Softball', etc.) |
| `titleExclusionSearch` | array | `` |  | Array of job titles to exclude. Use :* for prefix matching (e.g., 'Soft:*' will match 'Software', 'Softball', etc.) |
| `locationSearch` | array | `` |  | Array of locations to search for. Location search uses phrase matching, so you must use the exact 'City, State/Region, Country' format. All locations… |
| `locationExclusionSearch` | array | `` |  | Array of locations to exclude. Location search uses phrase matching, so you must use the exact 'City, State/Region, Country' format. All locations us… |
| `descriptionSearch` | array | `` |  | Warning: This search is NOT supported with the 6m backfill API. It might also be too intensive for the 7d API. Please be very specific, limit your se… |
| `descriptionExclusionSearch` | array | `` |  | Warning: This search is NOT supported with the 6m backfill API. It might also be too intensive for the 7d API. Please be very specific, limit your se… |
| `organizationSlugFilter` | array | `` |  | Filter by LinkedIn organization slugs (exact match). The slug is the company specific part of the url. For example the slug in the following url is '… |
| `organizationSlugExclusionFilter` | array | `` |  | Exclude jobs from specific LinkedIn organization slugs (exact match). The slug is the company specific part of the url. For example the slug in the f… |
| `organizationSearch` | array | `` |  | Array of organization names to search for. Use :* for prefix matching (e.g., 'Google:*' will match 'Google', 'Google Cloud', etc.) Getting several co… |
| `organizationExclusionSearch` | array | `` |  | Array of organization names to exclude. Use :* for prefix matching (e.g., 'Google:*' will match 'Google', 'Google Cloud', etc.) Getting several compa… |
| `organizationDescriptionSearch` | array | `` |  | Warning: This search is NOT supported with the 6m backfill API. Array of terms to search in organization description. Use :* for prefix matching (e.g… |
| `organizationDescriptionExclusionSearch` | array | `` |  | Warning: This search is NOT supported with the 6m backfill API. Array of terms to exclude from organization description. Use :* for prefix matching (… |
| `descriptionType` | string | `text` | enum: text, html | Format of the job description. Choose 'text' for plain text or 'html' for HTML formatted description. Leave empty to include no description. |
| `datePostedAfter` | string | `` |  | We don't recommend using this parameter if you retrieve jobs on a regular interval with the 1h/24h/7d time range. Filter jobs posted on or after a sp… |
| `remote` | boolean | `` |  | Filter for remote jobs only. Set to false to include all jobs. This filter is very sensitive and will include jobs that have 'remote' in the title, d… |
| `seniorityFilter` | array | `` |  | Filter English speaking countries by seniority level. This filter is case sensitive. You can select multiple seniority levels. |
| `externalApplyUrl` | boolean | `` |  | Filter for jobs that include an external apply URL (the opposited of Easy Apply) |
| `populateExternalApplyURL` | boolean | `False` |  | When enabled, populates the external_apply_url field with the url field value if external_apply_url is null |
| `noDirectApply` | boolean | `` |  | Filter for jobs that do not have easy apply on LinkedIn. |
| `directApply` | boolean | `` |  | Filter for jobs that can be applied to directly through LinkedIn Easy Apply |
| `industryFilter` | array | `` |  | Filter by LinkedIn industries. Use exact industry names. Industries containing commas will be automatically wrapped in quotes. You can find a list of… |
| `organizationEmployeesLte` | integer | `` | min 0 | Maximum number of employees in the company |
| `organizationEmployeesGte` | integer | `` | min 0 | Minimum number of employees in the company |
| `EmploymentTypeFilter` | array | `` |  | Filter by employment type |
| `excludeATSDuplicate` | boolean | `` |  | Set this parameter to true to remove the majority of duplicate jobs between this API and the 'Career Site Job Listing API' actor. LinkedIn jobs are c… |
| `populateAiRemoteLocation` | boolean | `False` |  | If enabled, populates ai_remote_location with locations_derived when ai_remote_location is empty. |
| `populateAiRemoteLocationDerived` | boolean | `False` |  | If enabled, populates ai_remote_location_derived with locations_derived when ai_remote_location_derived is empty. |
| `aiWorkArrangementFilter` | array | `` |  | BETA Feature: Filter by work arrangement. Remote OK = remote with an office available. Remote Solely = remote with no office available. Include both … |
| `aiHasSalary` | boolean | `` |  | BETA Feature: Filter for jobs with salary information only. Set to false to include all jobs. Results include jobs that have either an AI enriched sa… |
| `aiExperienceLevelFilter` | array | `` |  | BETA Feature: Filter by years of experience |
| `aiVisaSponsorshipFilter` | boolean | `` |  | BETA Feature: Filter for jobs offering visa sponsorship only. Set to false to include all jobs. |
| `aiTaxonomiesFilter` | array | `` |  | BETA Feature: Filter by AI taxonomies |
| `aiTaxonomiesPrimaryFilter` | array | `` |  | BETA Feature: Filter by primary AI taxonomy |
| `aiTaxonomiesExclusionFilter` | array | `` |  | BETA Feature: Exclude jobs by AI taxonomies. Warning: this filter is very broad and will exclude jobs that are not in any of the selected taxonomies.… |

### HuntBrief adapter hints

same Fantastic Jobs style as 54 Career Sites; add `remote` when Remote mode; use `limit` minimum 10 and `timeRange: 7d` for 1 week.

## Wellfound

- Mode: Standard
- Actor: `crawlerbros/wellfound-scraper`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `startUrls` | array | `` |  | Wellfound URLs. Default is https://wellfound.com/jobs. Supports ?remote=true and other query params. /role/l/ deep-filter URLs require residential pr… |
| `jobTitle` | string | `` |  | Client-side filter: only jobs whose title contains this phrase (case-insensitive). |
| `keyword` | string | `` |  | Client-side keyword filter across title + company + location (case-insensitive). |
| `location` | string | `` |  | Client-side location filter (case-insensitive match on any location name). |
| `remoteOnly` | boolean | `False` |  | Return only jobs flagged as remote. |
| `jobType` | string | `any` | enum: any, full-time, part-time, contract, internship | Filter by employment type (matches on job metadata/description). |
| `experience` | string | `any` | enum: any, junior, mid, senior, executive | Filter by experience level (matches on job title/description keywords). |
| `minSalary` | integer | `0` | min 0; max 1000000 | Minimum annual compensation parsed from job compensation field. |
| `maxSalary` | integer | `0` | min 0; max 1000000 | Maximum annual compensation. 0 = unlimited. |
| `includeNoSalary` | boolean | `True` |  | When false, jobs with no compensation field are filtered out. |
| `companyCategories` | array | `` |  | Filter companies by category (e.g. 'saas', 'fintech'). Case-insensitive substring match on startup name/slug. |
| `includeCompanies` | array | `` |  | Allow-list of company names/slugs (case-insensitive). |
| `excludeCompanies` | array | `` |  | Block-list of company names/slugs (case-insensitive). |
| `sort` | string | `none` | enum: newest, oldest, none | Sort by posted date (when available). |
| `maxItems` | integer | `50` | min 1; max 500 | Maximum jobs returned after filtering. |

### HuntBrief adapter hints

role → `jobTitle`; include keywords → `keyword`; remote mode → `remoteOnly`; seniority maps to `experience`; max → `maxItems`.

## YC Startup Jobs

- Mode: Standard
- Actor: `memo23/y-combinator-scraper`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `startUrls` | array | `[]` |  | Any YC URL. Auto-routed by path. Mix Jobs and Companies URLs freely. The two URLs prefilled below are sample defaults — clear them and paste your own… |
| `mode` | string | `jobs` | enum: jobs, companies | Pick a target surface; the matching filter fields below will be used. Jobs uses the role/location selectors; Companies uses the keyword/batch/industr… |
| `role` | string | `` | enum: , software-engineer, designer, product-manager, operations, marketing, sales-manager, recruiting-hr, support, science | Used when Mode = Jobs and Start URLs is empty. YC role category. |
| `location` | string | `` | enum: , san-francisco, new-york, los-angeles, seattle, austin, chicago, india, remote | Used when Mode = Jobs. City slugs match the city name in each job's location string; `remote` matches anything containing 'Remote'; `india` matches t… |
| `queries` | array | `[]` |  | Used when Mode = Companies. One or more keywords (product category, market, technology, location, …). Multiple keywords run as separate Algolia searc… |
| `topCompany` | boolean | `False` |  | Limit results to companies marked as Top by Y Combinator. Use when you want a smaller, more selective set. |
| `isHiring` | boolean | `False` |  | Limit results to companies currently marked as hiring. Useful for recruiting and active-growth research. |
| `nonprofit` | boolean | `False` |  | Limit results to companies marked as nonprofits. Leave disabled to include both commercial and nonprofit organizations. |
| `batch` | array | `["All Batches"]` |  | Select one or more batches to narrow results by cohort. |
| `industries` | array | `["All industries"]` |  | Select one or more industries to focus the run. |
| `regions` | array | `["Anywhere"]` |  | Select one or more regions to narrow geographically. |
| `minEmployeeSize` | string | `1+` | enum: 1+, 5+, 10+, 25+, 50+, 100+, 250+, 500+, 1000+ | Smallest team size to include. Select '1+' for no minimum. |
| `maxEmployeeSize` | string | `1000+` | enum: 1+, 5, 10, 25, 50, 100, 250, 500, 1000+ | Largest team size to include. Select '1000+' for no maximum. |
| `scrapeFounderDetails` | boolean | `False` |  | Companies mode only. Adds an extra HTTP per company (concurrency = 5) — slows runs. Fetches `/companies/{slug}` and adds `founders[]` (name, title, b… |
| `scrapeOpenJobs` | boolean | `False` |  | Companies mode only. Adds an extra HTTP per company (concurrency = 5) — slows runs. Fetches `/companies/{slug}/jobs` and adds `openJobs[]` (title, ro… |
| `maxItems` | integer | `100` | min 1 | **Per-URL cap.** Each entry in Start URLs (Option A) and each filter run (Option B) gets its own quota of this many records. Pasting 3 URLs with `max… |
| `monitoringMode` | boolean | `False` |  | Jobs scraper only: only jobs whose numeric id has not been stored in your Key-Value store yet are enqueued (per Apify user id). Useful for scheduled … |
| `maxConcurrency` | integer | `10` | min 1 | Jobs scraper only: max parallel HTTP requests. |
| `minConcurrency` | integer | `1` | min 1 | Jobs scraper only: min parallel HTTP requests to keep active. |
| `maxRequestRetries` | integer | `3` | min 0 | Jobs scraper only: retries before a request is marked failed. |
| `proxy` | object | `{"useApifyProxy": true}` |  | Proxy for the Jobs scraper. Companies mode hits YC's Algolia API directly and does not use a proxy regardless of what's configured here. |

### HuntBrief adapter hints

use `mode: jobs`; roles/keywords → `queries`; remote mode can use `location: remote`; max → `maxItems`.

## Welcome to the Jungle

- Mode: Standard
- Actor: `shahidirfan/jungle-job-scraper`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `keyword` | string | `` |  | Job title, skill, or keyword to search for (e.g., 'Software Engineer', 'Marketing Manager', 'Data Analyst', 'Frontend Developer'). Leave empty to sea… |
| `location` | string | `` |  | Two-letter country code to filter jobs (e.g., 'US', 'GB', 'FR', 'DE'). Leave empty for worldwide results. |
| `posted_within` | string | `any` | enum: any, 24h, 7d, 30d | Filter jobs by posting recency. |
| `results_wanted` | integer | `20` |  | Maximum number of job listings to extract. Set a higher value to collect more jobs. |
| `max_pages` | integer | `5` |  | Safety limit on search result pages to process (prevents infinite loops). |

### HuntBrief adapter hints

role/keywords → `keyword`; location → `location`; posted within 1 week → `posted_within: 7d`; max → `results_wanted`.

## HiringCafe

- Mode: Remote
- Actor: `memo23/apify-hiring-cafe-scraper`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `startUrls` | array | `` |  | Listing pages (`?searchState=…`) or job detail URLs (`/viewjob/…`). **If any URL is set, these are used and Keyword / Location / Workplace are ignore… |
| `keyword` | string | `` |  | Search query → `searchState.searchQuery`. **Only used when Start URLs are empty.** |
| `location` | string | `` |  | United States (default) uses the site’s country preset. **Only used when Start URLs are empty.** For exact regions (e.g. Florida with place id), past… |
| `workplaceType` | string | `Any` | enum: Any, Remote, Hybrid, Onsite | Remote, Hybrid, Onsite, or Any (all three). **Only used when Start URLs are empty** (Any + default location + empty keyword does not enable filter mo… |
| `maxItems` | integer | `1000` |  | Maximum number of jobs to scrape. |
| `maxConcurrency` | integer | `100` |  | Maximum number of pages that can be processed at the same time. |
| `minConcurrency` | integer | `1` |  | Minimum number of pages that will be processed at the same time. |
| `maxRequestRetries` | integer | `200` |  | Number of times the crawler will retry a failed request before giving up. |
| `flattenOutput` | boolean | `False` |  | If on, nested objects in each job record are flattened with underscores (e.g. <code>enriched_company_data.name</code> becomes <code>enriched_company_… |
| `enrichDescription` | boolean | `True` |  | When on (default), the actor fetches each job's detail page after the listing to populate <code>job_information.description</code> with the full HTML… |
| `proxy` | object | `{"useApifyProxy": true, "apifyProxyGroups": ["RESIDENTIAL"]}` |  | Specifies proxy servers that will be used by the scraper in order to hide its origin.<br><br>For details, see <a href='https://apify.com/apify/web-sc… |

### HuntBrief adapter hints

roles/keywords → `keyword`; locations → `location`; remote mode → `workplaceType: Remote`; max → `maxItems`; use `flattenOutput` for easier output.

## We Work Remotely

- Mode: Remote
- Actor: `shahidirfan/weworkremotely-job-scrapper`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `search_urls` | array | `` |  | Optional list of search URLs. Terms from all URLs are used as OR filters. |
| `category` | string | `all` | enum: all, remote-full-stack-programming-jobs, remote-front-end-programming-jobs, remote-back-end-programming-jobs, remote-design-jobs, remote-devops-sysadmin-jobs, remote-managem… | Select which RSS API feed to use. 'all' uses the global feed with all categories. |
| `results_wanted` | integer | `20` | min 1 | Maximum number of jobs to return from the selected RSS API feed. |

### HuntBrief adapter hints

limited structured filters; use `category: all` unless UI adds category; max → `results_wanted`; post-filter by roles/keywords/location.

## 4 Day Week

- Mode: Remote
- Actor: `crawlerbros/four-day-week-jobs-scraper`
- Required input fields: `mode`

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `mode` | string | `browse` | enum: search, browse | How to fetch jobs. Use 'search' to search by keyword or filter; use 'browse' to retrieve all available jobs. |
| `query` | string | `` |  | Text keyword to search job titles and descriptions (mode=search). Example: 'python', 'react', 'data scientist'. |
| `category` | string | `` | enum: , software-dev, design, marketing, customer-support, sales, data, product, business, finance, hr, qa… | Filter jobs by category. |
| `jobType` | string | `` | enum: , full_time, part_time, contract, freelance | Filter by employment type. |
| `maxItems` | integer | `100` | min 1; max 10000 | Maximum number of job records to return. |

### HuntBrief adapter hints

use required `mode: search`; roles/keywords → `query`; max → `maxItems`; category/jobType optional.

## FlexJobs

- Mode: Remote
- Actor: `stealth_mode/flexjobs-jobs-search-scraper`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `urls` | array | `` |  | Add the URLs of the Jobs list urls you want to scrape. You can paste URLs one by one, or use the Bulk edit section to add a prepared list. |
| `ignore_url_failures` | boolean | `` |  | If true, the scraper will continue running even if some URLs fail to be scraped. |
| `max_items_per_url` | integer | `` |  | The maximum number of items to scrape per URL. |

### HuntBrief adapter hints

API is URL-driven; build FlexJobs search URL(s) in `urls`; max per URL → `max_items_per_url`; use `ignore_url_failures`.

## Himalayas

- Mode: Remote
- Actor: `inlifeprojects/himalayas-jobs-scraper`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `keywords` | array | `["software engineer"]` |  | Keywords to search for. Each keyword runs a separate API search and results are deduplicated. E.g. ['react engineer', 'python developer', 'devops'] |
| `seniority` | array | `["Senior", "Mid-level", "Entry-level"]` |  | Filter by seniority. Accepted values: Entry-level, Mid-level, Senior, Manager, Director, Executive. Leave empty for all levels. |
| `employmentType` | string | `Full Time` | enum: Full Time, Part Time, Contractor, Temporary, Intern, Volunteer, Other, | Filter by employment type. |
| `worldwide` | boolean | `True` |  | When true, only returns jobs explicitly open to candidates worldwide. Recommended for global talent platforms. |
| `country` | string | `` |  | Filter by country using ISO alpha-2 code, common name, or slug. E.g. 'US', 'Germany', 'spain'. Only used when 'Worldwide only' is false. |
| `sortBy` | string | `recent` | enum: recent, relevant, salaryAsc, salaryDesc, nameAToZ, nameZToA | Sort order for results returned by the Himalayas API. |
| `maxResultsPerKeyword` | integer | `100` | min 1; max 500 | Maximum number of jobs to collect per keyword. Himalayas returns max 20 per page. Set lower for faster runs. |
| `filterNonTech` | boolean | `False` |  | When true, removes clearly non-technical roles such as receptionist, accountant, driver, nurse etc. from results. |
| `webhookUrl` | string | `` |  | If provided, POSTs all results to this URL in batches of 50 when the Actor completes. Ideal for job boards and data pipelines. |
| `webhookSecret` | string | `` |  | If provided alongside webhookUrl, sent as X-Scraper-Secret header to authenticate webhook delivery. |

### HuntBrief adapter hints

roles → `keywords`; seniority → `seniority`; country/worldwide maps to `country`/`worldwide`; max → `maxResultsPerKeyword`.

## Just Remote

- Mode: Remote
- Actor: `kinaesthetic_millionaire/justremote`
- Required input fields: `inputUrls`

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `inputUrls` | array | `` |  | JustRemote jobs listing or detail URLs to scrape. |
| `scrapeCompanyInfo` | boolean | `False` |  | Scrape company information like title, description, active listings etc. |
| `maxResults` | integer | `5` |  | Maximum number of job listings to scrape. |
| `enableCache` | boolean | `False` |  | Skip already-scraped job URLs from previous runs. Uses a persistent Key-Value Store to remember scraped jobs by URL. |
| `proxyConfiguration` | object | `` |  | Proxy settings for the scraper. |

### HuntBrief adapter hints

Map HuntBrief fields to the closest actor fields and post-filter unsupported filters.

## Remotive

- Mode: Remote
- Actor: `santamaria-automations/remotive-scraper`
- Required input fields: none declared

| Field | Type | Default | Enum / limits | Notes |
|---|---|---|---|---|
| `searchUrls` | array | `` |  | Paste one or more Remotive URLs. Supports category URLs (remotive.com/remote-jobs/software-dev) and filter URLs with query params (remotive.com/remot… |
| `searchQueries` | array | `` |  | One or more search terms or category keywords (e.g. 'software-dev', 'design', 'golang', 'react developer'). Each runs as a separate search. Results a… |
| `includeCompanyInfo` | boolean | `False` |  | Fetch company profile pages to enrich results with company website, tagline, open positions count, and profile URL. Adds ~0.5s per unique company. |
| `maxResultsPerQuery` | integer | `100` | min 1 | Maximum results per search URL or keyword. |
| `maxResults` | integer | `10` | min 0 | Total cap across all queries and search URLs. Set to 0 for unlimited. |
| `proxyConfiguration` | object | `{"useApifyProxy": true, "apifyProxyGroups": []}` |  | Proxy configuration. Default uses Apify datacenter proxies. |

### HuntBrief adapter hints

roles/keywords → `searchQueries`; max → `maxResultsPerQuery` and `maxResults`; include company info if useful.

