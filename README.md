# Drop the Grind

Drop the Grind is a local-first macOS desktop app for human-reviewed job discovery and application prep. It turns job-source outputs into a structured workspace where users can review opportunities, manage local files, and generate application packet tasks while keeping the resume as the source of truth and avoiding auto-apply automation.
